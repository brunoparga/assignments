extends layouts/main-layout.pug

block content
  if users.length > 0
    ul
      each user in users
        li #{user} 
          a(href=`/delete?user=${user}`) #[small (delete)]
  else
    h2 No users found!
  
  a(href="/") Add user
